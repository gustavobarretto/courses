const soma = (n1, n2) => n1 + n2
const subtracao = (n1, n2) => n1 - n2
const multiplicacao = (n1, n2) => n1 * n2
const divisao = (n1, n2) => n1 / n2

console.log(soma(10, 5), subtracao(10, 5), multiplicacao(10, 5), divisao(10, 5))
